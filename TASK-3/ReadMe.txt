1) Before you run the file make sure you have installed following library in your workspace or environment.


pip install numpy
pip install pandas
pip install tensorflow
pip install matplotlib
pip install scikiplot
pip install seaborn
pip install folium
pip install warnings
pip install IPython

2) download and extract the task-3.rar file to access the dataset and jupyter file.


3) Make sure terrorism dataset and jupyter file are located in same folder. 
